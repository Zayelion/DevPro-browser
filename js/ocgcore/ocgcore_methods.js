//duel.cpp

duel.duel.prototype = function() {
	lua                  = new interpreter(this);
	game_field           = new field(this);
	game_field.temp_card = new_card(0);
	bufferlen            = 0;
	bufferp              = buffer;
}
duel_.duel.prototype = function(){
	
}